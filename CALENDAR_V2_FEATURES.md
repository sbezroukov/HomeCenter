# 🎉 Календарь v2.0 - Новые функции

## Обзор обновления

Добавлены расширенные функции для календаря с расписанием, включая копирование расписания, статистику, подтверждение выполнения, фото, комментарии и интеграцию с тестами.

---

## ✨ Новые функции

### 1. **Копирование и перенос расписания**

#### Копирование с прошлой недели
- Быстрое копирование всего расписания с предыдущей недели
- Сохраняются все параметры задач (тип, время, ответственный)
- Автоматическая корректировка дат и дедлайнов

**Использование:**
```
В календаре → Кнопка "Копировать с прошлой недели"
```

#### Копирование дня
- Копирование всех задач с одного дня на другой
- Полезно для повторяющихся расписаний

**API:**
```http
POST /Calendar/CopyDay
{
  "sourceDate": "2026-02-26",
  "targetDate": "2026-03-05"
}
```

#### Перенос дня (только для Admin)
- Перемещение всех задач с одного дня на другой
- Задачи удаляются с исходной даты

---

### 2. **Детальная статистика выполнения**

#### Общая статистика
- Всего заданий
- Выполнено / Не выполнено / Частично выполнено
- В срок / С опозданием
- Подтверждено / Ожидает подтверждения

#### Статистика по типам деятельности
- Количество заданий по каждому типу
- Процент выполнения
- Визуализация с прогресс-барами

#### Статистика по пользователям (для Admin)
- Рейтинг пользователей по выполнению
- Процент успешности каждого пользователя
- Сравнительный анализ

**Доступ:**
```
Календарь → Статистика
```

**API:**
```http
GET /api/calendar/statistics?from=2026-01-01&to=2026-02-26
```

---

### 3. **Роль Supervisor и подтверждение выполнения**

#### Новая роль: Supervisor
- Может подтверждать или отклонять выполнение задач
- Доступ к списку заданий, ожидающих подтверждения
- Может оставлять комментарии при отклонении

#### Процесс подтверждения
1. Пользователь отмечает задачу как выполненную
2. Supervisor получает уведомление
3. Supervisor подтверждает или отклоняет с указанием причины
4. Статус обновляется в системе

**Поля в ActivityCompletion:**
- `IsApprovedBySupervisor` - подтверждено ли
- `ApprovedByUserId` - кто подтвердил
- `ApprovedAt` - когда подтверждено

**Доступ:**
```
Supervisor → Ожидают подтверждения
```

---

### 4. **Прикрепление фото к заданиям**

#### Возможности
- Загрузка фотографий к любой активности
- Несколько фото на одну задачу
- Описание для каждого фото
- Автоматическое сохранение метаданных (размер, тип, дата)

#### Модель ActivityPhoto
```csharp
{
  Id: int,
  ScheduledActivityId: int,
  UploadedByUserId: int,
  FilePath: string,
  OriginalFileName: string,
  FileSize: long,
  ContentType: string,
  Description: string?,
  UploadedAt: DateTime
}
```

**Хранение:**
- Файлы: `/wwwroot/uploads/activity-photos/`
- Формат имени: `{activityId}_{guid}.{extension}`

**API:**
```http
POST /api/calendar/{activityId}/photos
Content-Type: multipart/form-data

GET /api/calendar/{activityId}/photos

DELETE /api/calendar/photos/{photoId}
```

---

### 5. **Комментарии к заданиям**

#### Возможности
- Добавление комментариев к активностям
- Ответы на комментарии (вложенность)
- Удаление комментариев (мягкое удаление)
- Редактирование комментариев

#### Модель ActivityComment
```csharp
{
  Id: int,
  ScheduledActivityId: int,
  AuthorUserId: int,
  Text: string,
  ParentCommentId: int?,
  CreatedAt: DateTime,
  UpdatedAt: DateTime?,
  IsDeleted: bool
}
```

**API:**
```http
POST /api/calendar/{activityId}/comments
{
  "text": "Комментарий",
  "parentCommentId": null
}

GET /api/calendar/{activityId}/comments

DELETE /api/calendar/comments/{commentId}
```

---

### 6. **Интеграция с тестами**

#### Связь активностей с тестами
- При создании активности можно выбрать тест
- Поле `TestTopicId` в `ScheduledActivity`
- Автоматическая связь результата теста с выполнением

#### Отображение результатов
- Процент выполнения теста
- Статус на основе результата (>95% = успешно)
- История попыток

#### Автоматическое определение статуса
```
>= 95% → Выполнено (Completed)
>= 50% → Частично выполнено (PartiallyCompleted)
< 50%  → Не выполнено (NotCompleted)
```

#### Модель связи
```csharp
ActivityCompletion {
  TestAttemptId: int?,
  TestAttempt: TestAttempt?
}

ScheduledActivity {
  TestTopicId: int?,
  TestTopic: Topic?
}
```

**API:**
```http
GET /api/calendar/{activityId}/test-result?userId={userId}

POST /api/calendar/completions/{completionId}/link-test
{
  "testAttemptId": 123
}
```

---

## 📊 Обновленная база данных

### Новые таблицы

#### ActivityPhotos
```sql
CREATE TABLE ActivityPhotos (
  Id INTEGER PRIMARY KEY AUTOINCREMENT,
  ScheduledActivityId INTEGER NOT NULL,
  UploadedByUserId INTEGER NOT NULL,
  FilePath TEXT NOT NULL,
  OriginalFileName TEXT,
  FileSize INTEGER NOT NULL,
  ContentType TEXT,
  Description TEXT,
  UploadedAt TEXT NOT NULL,
  FOREIGN KEY (ScheduledActivityId) REFERENCES ScheduledActivities(Id) ON DELETE CASCADE,
  FOREIGN KEY (UploadedByUserId) REFERENCES Users(Id)
);
```

#### ActivityComments
```sql
CREATE TABLE ActivityComments (
  Id INTEGER PRIMARY KEY AUTOINCREMENT,
  ScheduledActivityId INTEGER NOT NULL,
  AuthorUserId INTEGER NOT NULL,
  Text TEXT NOT NULL,
  ParentCommentId INTEGER,
  CreatedAt TEXT NOT NULL,
  UpdatedAt TEXT,
  IsDeleted INTEGER NOT NULL DEFAULT 0,
  FOREIGN KEY (ScheduledActivityId) REFERENCES ScheduledActivities(Id) ON DELETE CASCADE,
  FOREIGN KEY (AuthorUserId) REFERENCES Users(Id),
  FOREIGN KEY (ParentCommentId) REFERENCES ActivityComments(Id)
);
```

### Новые поля в существующих таблицах

#### ScheduledActivities
- `TestTopicId` - связь с тестом

#### ActivityCompletions
- `IsApprovedBySupervisor` - подтверждено ли
- `ApprovedByUserId` - кто подтвердил
- `ApprovedAt` - когда подтверждено
- `TestAttemptId` - связь с результатом теста

---

## 🔧 Новые API Endpoints

### Копирование и перенос
```http
POST /Calendar/CopyFromPreviousWeek?weekStart={date}
POST /Calendar/CopyDay?sourceDate={date}&targetDate={date}
POST /Calendar/MoveDay?sourceDate={date}&targetDate={date}  # Admin only
```

### Статистика
```http
GET /Calendar/Statistics?from={date}&to={date}
GET /api/calendar/statistics?from={date}&to={date}
```

### Supervisor
```http
GET /Supervisor/PendingApprovals
POST /Supervisor/Approve?completionId={id}
POST /Supervisor/Reject?completionId={id}&reason={text}
```

### Фото
```http
POST /api/calendar/{activityId}/photos
GET /api/calendar/{activityId}/photos
DELETE /api/calendar/photos/{photoId}
```

### Комментарии
```http
POST /api/calendar/{activityId}/comments
GET /api/calendar/{activityId}/comments
DELETE /api/calendar/comments/{commentId}
```

### Тесты
```http
GET /api/calendar/tests
GET /api/calendar/{activityId}/test-result?userId={userId}
POST /api/calendar/completions/{completionId}/link-test
```

---

## 🎯 Сценарии использования

### Сценарий 1: Еженедельное расписание
1. Создайте расписание на неделю
2. На следующей неделе нажмите "Копировать с прошлой недели"
3. Отредактируйте при необходимости
4. Готово!

### Сценарий 2: Тест как задание
1. Создайте активность типа "Тест"
2. Выберите тест из списка
3. Назначьте ответственного
4. Пользователь проходит тест
5. Результат автоматически связывается с активностью
6. Статус определяется по проценту (>95% = успешно)

### Сценарий 3: Подтверждение выполнения
1. Пользователь выполняет задачу
2. Прикрепляет фото результата
3. Отмечает как выполненную
4. Supervisor получает уведомление
5. Supervisor проверяет фото и подтверждает
6. Статус обновляется

### Сценарий 4: Обсуждение задачи
1. Создается задача
2. Пользователи оставляют комментарии
3. Можно отвечать на комментарии
4. История обсуждения сохраняется

---

## 📈 Статистика и отчеты

### Доступные метрики
- Общее количество заданий
- Процент выполнения
- Своевременность выполнения
- Статус подтверждения
- Распределение по типам
- Рейтинг пользователей

### Фильтры
- По датам (от/до)
- По пользователю
- По типу деятельности
- По статусу

---

## 🔐 Роли и права доступа

### User (обычный пользователь)
- Просмотр своих задач
- Отметка выполнения
- Прикрепление фото
- Комментирование
- Прохождение тестов
- Просмотр своей статистики

### Supervisor
- Все права User
- Подтверждение выполнения
- Отклонение выполнения
- Просмотр заданий всех пользователей

### Admin
- Все права Supervisor
- Создание/редактирование задач
- Управление типами деятельности
- Копирование и перенос расписания
- Просмотр статистики всех пользователей
- Управление пользователями

---

## 🚀 Миграция с v1.0

Все изменения обратно совместимы. При запуске приложения:
1. Автоматически создаются новые таблицы
2. Добавляются новые поля в существующие таблицы
3. Старые данные сохраняются

**Действия после обновления:**
1. Запустите приложение
2. Проверьте логи на успешную миграцию
3. Назначьте роль Supervisor нужным пользователям (через SQL):
   ```sql
   -- Пока роль Supervisor = Admin, можно расширить позже
   ```

---

## 📝 Changelog

### v2.0.0 (26.02.2026)

**Добавлено:**
- ✅ Копирование расписания с прошлой недели
- ✅ Копирование и перенос дня
- ✅ Детальная статистика выполнения
- ✅ Роль Supervisor
- ✅ Подтверждение выполнения задач
- ✅ Прикрепление фото к заданиям
- ✅ Комментарии к заданиям
- ✅ Интеграция с тестами
- ✅ Автоматическое определение успешности тестов (>95%)
- ✅ API endpoints для всех новых функций

**Улучшено:**
- Расширена модель данных
- Добавлены новые индексы для производительности
- Улучшена навигация

**Исправлено:**
- Совместимость с существующей системой тестов

---

## 🎨 UI/UX улучшения

- Кнопка "Копировать с прошлой недели" в календаре
- Страница статистики с графиками и таблицами
- Индикаторы подтверждения выполнения
- Галерея фотографий в задачах
- Ветка комментариев с ответами
- Отображение результатов тестов
- Цветовая индикация статусов

---

## 📚 Дополнительная документация

- `CALENDAR_README.md` - основная документация v1.0
- `CALENDAR_QUICKSTART.md` - быстрый старт
- `CALENDAR_ARCHITECTURE.md` - техническая архитектура
- `CALENDAR_V2_FEATURES.md` - этот документ

---

**Версия:** 2.0.0  
**Дата релиза:** 26.02.2026  
**Статус:** ✅ Готово к использованию
