# 📅 Календарь с расписанием - Документация

## Обзор функционала

Система календаря с расписанием позволяет планировать и отслеживать выполнение задач с уведомлениями через Telegram Bot.

## Основные возможности

### 1. **Справочник видов деятельности**
- Создание типов задач (например: Уборка, Учеба, Работа, Спорт)
- Настройка цвета для каждого типа
- Активация/деактивация типов
- Доступ: только для администраторов

### 2. **Планирование активностей**
- Создание задач на конкретный день
- Указание времени начала и окончания (или задача на весь день)
- Назначение ответственного исполнителя
- Установка дедлайна для завершения
- Повторяющиеся задачи (еженедельно)
- Добавление описания и комментариев

### 3. **Недельный календарь**
- Просмотр расписания на неделю
- Навигация между неделями
- Цветовая индикация типов задач
- Отображение статуса выполнения
- Быстрое создание задач на конкретный день

### 4. **Отметка выполнения**
- Статусы: Выполнена, Не выполнена, Частично выполнена, Отменена
- Указание причины невыполнения
- Автоматическое определение своевременности выполнения

### 5. **Telegram уведомления**
Автоматические уведомления о:
- Начале задачи (за 15 минут до старта)
- Просроченных задачах (раз в день)
- Завершении задачи (с указанием статуса)
- Незакрытых задачах (через час после дедлайна)

### 6. **API для интеграций**
REST API endpoints для:
- Получения списка задач
- Отметки выполнения
- Получения статистики
- Просмотра просроченных и предстоящих задач

## Настройка

### 1. Конфигурация Telegram Bot

Добавьте в файл `.env`:

```env
Telegram__BotToken=YOUR_BOT_TOKEN_HERE
Telegram__Enabled=true
```

Или установите переменные окружения:
```bash
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
TELEGRAM_ENABLED=true
```

### 2. Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в конфигурацию

### 3. Привязка пользователя к Telegram

Для получения уведомлений пользователю нужно:

1. Найти своего бота в Telegram
2. Отправить команду `/start`
3. Узнать свой Chat ID (можно использовать [@userinfobot](https://t.me/userinfobot))
4. Администратор должен добавить Chat ID в профиль пользователя в базе данных

**SQL запрос для обновления:**
```sql
UPDATE Users SET TelegramChatId = YOUR_CHAT_ID WHERE UserName = 'username';
```

## Структура базы данных

### Таблица ActivityTypes
- `Id` - идентификатор
- `Name` - название типа деятельности
- `Description` - описание
- `Color` - цвет в формате HEX (#RRGGBB)
- `IsActive` - активен ли тип
- `CreatedAt`, `UpdatedAt` - временные метки

### Таблица ScheduledActivities
- `Id` - идентификатор
- `ActivityTypeId` - тип деятельности
- `Title` - название (опционально)
- `Description` - описание
- `StartDate` - дата начала
- `StartTime` - время начала (nullable)
- `EndTime` - время окончания (nullable)
- `DeadlineDateTime` - дедлайн
- `AssignedToUserId` - ответственный
- `CreatedByUserId` - создатель
- `IsRecurring` - повторяющаяся задача
- `RecurringDayOfWeek` - день недели для повторения
- `IsActive` - активна ли задача
- `CreatedAt`, `UpdatedAt` - временные метки

### Таблица ActivityCompletions
- `Id` - идентификатор
- `ScheduledActivityId` - связанная активность
- `CompletedByUserId` - кто завершил
- `Status` - статус выполнения (1-4)
- `Comment` - комментарий
- `CompletedAt` - время завершения
- `IsOnTime` - выполнено в срок

### Обновление таблицы Users
- `TelegramChatId` - Telegram Chat ID для уведомлений

## API Endpoints

### GET /api/calendar/week?date=2026-02-26
Получить активности на неделю

### GET /api/calendar/day?date=2026-02-26
Получить активности на день

### GET /api/calendar/{id}
Получить конкретную активность

### POST /api/calendar/{id}/complete
Отметить выполнение активности

**Body:**
```json
{
  "status": 1,
  "comment": "Выполнено успешно"
}
```

**Статусы:**
- 1 - Completed (Выполнена)
- 2 - NotCompleted (Не выполнена)
- 3 - PartiallyCompleted (Частично выполнена)
- 4 - Cancelled (Отменена)

### GET /api/calendar/overdue
Получить просроченные активности

### GET /api/calendar/upcoming?hours=24
Получить предстоящие активности

### GET /api/calendar/statistics?from=2026-01-01&to=2026-02-26
Получить статистику выполнения

## Использование

### Для администратора:

1. **Настройка типов деятельности:**
   - Перейдите в "Типы деятельности" в меню
   - Создайте необходимые типы (Уборка, Учеба, Работа и т.д.)
   - Выберите цвета для визуального различия

2. **Создание задач:**
   - Откройте "Календарь"
   - Нажмите "Добавить активность"
   - Заполните форму и назначьте ответственного

3. **Мониторинг:**
   - Просматривайте недельное расписание
   - Отслеживайте просроченные задачи
   - Проверяйте статистику выполнения

### Для пользователя:

1. **Просмотр задач:**
   - Откройте "Календарь"
   - Просматривайте свои задачи на неделю
   - Используйте навигацию между неделями

2. **Выполнение задач:**
   - Нажмите кнопку "✓" на задаче
   - Выберите статус выполнения
   - Добавьте комментарий при необходимости

3. **Получение уведомлений:**
   - Настройте Telegram Chat ID
   - Получайте уведомления о задачах
   - Реагируйте на напоминания

## Фоновая служба

Служба `CalendarNotificationBackgroundService` работает каждые 5 минут и:

1. Отправляет уведомления о начале задач (за 15 минут)
2. Напоминает о просроченных задачах (раз в день)
3. Уведомляет о незакрытых задачах (через час после дедлайна)

## Безопасность

- Все endpoints требуют авторизации
- Пользователи видят только свои задачи (или все, если администратор)
- Редактирование доступно только создателю или администратору
- Управление типами деятельности - только для администраторов

## Расширение функционала

### Возможные улучшения:

1. **Telegram Bot команды:**
   - `/mytasks` - список задач на сегодня
   - `/complete {id}` - отметить выполнение
   - `/upcoming` - предстоящие задачи

2. **Дополнительные уведомления:**
   - Email уведомления
   - Push уведомления в браузере
   - SMS уведомления

3. **Расширенная аналитика:**
   - Графики выполнения задач
   - Отчеты по типам деятельности
   - Сравнение эффективности пользователей

4. **Интеграции:**
   - Экспорт в Google Calendar
   - Импорт из других систем
   - Webhook для внешних систем

## Troubleshooting

### Уведомления не приходят:

1. Проверьте, что `Telegram__Enabled=true`
2. Проверьте корректность Bot Token
3. Убедитесь, что у пользователя установлен TelegramChatId
4. Проверьте логи приложения на ошибки

### Задачи не отображаются:

1. Проверьте, что задача активна (IsActive=true)
2. Убедитесь, что дата задачи в нужном диапазоне
3. Проверьте права доступа пользователя

### База данных не обновляется:

1. Убедитесь, что миграция выполнена при старте приложения
2. Проверьте логи на ошибки при создании таблиц
3. При необходимости удалите quiz.db и перезапустите приложение

## Контакты и поддержка

При возникновении проблем проверьте логи приложения и базу данных.
