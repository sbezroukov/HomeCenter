# 🚀 Быстрый старт - Календарь с расписанием

## Шаг 1: Запуск приложения

```bash
cd HomeCenter
dotnet run
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Шаг 2: Вход в систему

1. Откройте браузер и перейдите на `http://localhost:5000`
2. Войдите как администратор (используйте учетные данные из `.env`)
3. Или зарегистрируйте нового пользователя

## Шаг 3: Настройка типов деятельности

1. В меню выберите **"Типы деятельности"** (доступно только администраторам)
2. Создайте несколько типов, например:
   - 🏠 Уборка (цвет: #FF5733)
   - 📚 Учеба (цвет: #3498DB)
   - 💼 Работа (цвет: #2ECC71)
   - 🏃 Спорт (цвет: #F39C12)
   - 🍳 Готовка (цвет: #E74C3C)

## Шаг 4: Создание первой задачи

1. Перейдите в **"📅 Календарь"**
2. Нажмите **"Добавить активность"**
3. Заполните форму:
   - Выберите тип деятельности
   - Укажите дату и время (или оставьте пустым для задачи на весь день)
   - Назначьте ответственного (опционально)
   - Установите дедлайн (опционально)
4. Нажмите **"Создать"**

## Шаг 5: Работа с календарем

### Просмотр расписания
- Календарь показывает текущую неделю
- Используйте стрелки для навигации между неделями
- Задачи отображаются с цветовой индикацией по типам

### Отметка выполнения
1. Найдите задачу в календаре
2. Нажмите кнопку **"✓"**
3. Выберите статус:
   - ✓ Выполнена
   - ✗ Не выполнена
   - ◐ Частично выполнена
   - ⊘ Отменена
4. Добавьте комментарий (опционально)
5. Сохраните

### Редактирование и удаление
- Кнопка **"✎"** - редактировать задачу
- Кнопка **"✕"** - удалить задачу

## Шаг 6: Настройка Telegram уведомлений (опционально)

### 6.1 Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "HomeCenter Calendar Bot")
4. Придумайте username (например: "homecenter_calendar_bot")
5. Скопируйте полученный токен

### 6.2 Настройка в приложении

Добавьте в файл `.env`:

```env
Telegram__BotToken=YOUR_BOT_TOKEN_HERE
Telegram__Enabled=true
```

Перезапустите приложение.

### 6.3 Получение Chat ID

1. Найдите своего бота в Telegram
2. Отправьте команду `/start`
3. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
4. Отправьте ему любое сообщение
5. Скопируйте ваш Chat ID (число)

### 6.4 Привязка Chat ID к пользователю

**Вариант 1: Через SQL (для администратора)**

```bash
# Откройте базу данных
sqlite3 quiz.db

# Обновите Chat ID для пользователя
UPDATE Users SET TelegramChatId = YOUR_CHAT_ID WHERE UserName = 'username';

# Проверьте
SELECT UserName, TelegramChatId FROM Users;

# Выход
.quit
```

**Вариант 2: Через код (требуется добавить функционал в UI)**

Можно добавить страницу профиля, где пользователь сможет указать свой Chat ID.

### 6.5 Проверка уведомлений

После настройки вы будете получать уведомления:
- 🔔 За 15 минут до начала задачи
- ⚠️ О просроченных задачах (раз в день)
- ✅ О завершении задачи
- ⚠️ О незакрытых задачах (через час после дедлайна)

## Шаг 7: Использование API (опционально)

API доступен по адресу `/api/calendar`

### Примеры запросов:

**Получить задачи на неделю:**
```bash
curl -X GET "http://localhost:5000/api/calendar/week?date=2026-02-26" \
  -H "Cookie: .AspNetCore.Cookies=YOUR_AUTH_COOKIE"
```

**Отметить выполнение:**
```bash
curl -X POST "http://localhost:5000/api/calendar/123/complete" \
  -H "Content-Type: application/json" \
  -H "Cookie: .AspNetCore.Cookies=YOUR_AUTH_COOKIE" \
  -d '{"status": 1, "comment": "Выполнено успешно"}'
```

**Получить просроченные задачи:**
```bash
curl -X GET "http://localhost:5000/api/calendar/overdue" \
  -H "Cookie: .AspNetCore.Cookies=YOUR_AUTH_COOKIE"
```

## Полезные советы

### Повторяющиеся задачи
Используйте флаг "Повторяющаяся задача" для еженедельных активностей (например, уборка каждый понедельник).

### Задачи на весь день
Оставьте поле "Время начала" пустым, чтобы создать задачу на весь день.

### Цветовое кодирование
Используйте разные цвета для типов деятельности, чтобы быстро ориентироваться в расписании.

### Дедлайны
Устанавливайте дедлайны для важных задач - система будет отслеживать просрочки.

## Устранение проблем

### Приложение не запускается
```bash
# Проверьте наличие .NET 8.0
dotnet --version

# Восстановите зависимости
dotnet restore

# Пересоберите проект
dotnet build
```

### База данных не создается
```bash
# Удалите старую базу (если есть)
rm quiz.db

# Перезапустите приложение
dotnet run
```

### Уведомления не работают
1. Проверьте `.env` файл
2. Убедитесь, что `Telegram__Enabled=true`
3. Проверьте корректность Bot Token
4. Убедитесь, что TelegramChatId установлен для пользователя

## Следующие шаги

- Изучите полную документацию в `CALENDAR_README.md`
- Настройте повторяющиеся задачи для регулярных активностей
- Интегрируйте API в свои приложения
- Настройте Telegram уведомления для всех пользователей

---

**Приятного использования! 🎉**
