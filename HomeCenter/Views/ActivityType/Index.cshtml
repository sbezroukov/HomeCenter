@model List<ActivityType>
@{
    ViewData["Title"] = "Типы деятельности";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📋 Справочник типов деятельности</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Добавить тип
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            <p class="mb-0">Нет типов деятельности. Создайте первый тип для начала работы с календарем.</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th style="width: 50px;">Цвет</th>
                        <th>Название</th>
                        <th>Описание</th>
                        <th style="width: 100px;">Статус</th>
                        <th style="width: 150px;">Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var type in Model)
                    {
                        <tr class="@(!type.IsActive ? "table-secondary" : "")">
                            <td>
                                <div style="width: 30px; height: 30px; background-color: @type.Color; border-radius: 4px; border: 1px solid #ddd;"></div>
                            </td>
                            <td>
                                <strong>@type.Name</strong>
                            </td>
                            <td>
                                @(type.Description ?? "-")
                            </td>
                            <td>
                                @if (type.IsActive)
                                {
                                    <span class="badge bg-success">Активен</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Неактивен</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@type.Id" class="btn btn-sm btn-outline-primary">
                                        Изменить
                                    </a>
                                    @if (type.IsActive)
                                    {
                                        <form asp-action="Delete" asp-route-id="@type.Id" method="post" 
                                              onsubmit="return confirm('Деактивировать этот тип деятельности?');" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-outline-danger">
                                                Деактивировать
                                            </button>
                                        </form>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <div class="mt-3">
        <a asp-controller="Calendar" asp-action="Index" class="btn btn-secondary">← Вернуться к календарю</a>
    </div>
</div>
