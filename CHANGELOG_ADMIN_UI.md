# Изменения: Улучшение UI админки

## Что изменилось

### 📊 Компактное отображение имен файлов

В админке теперь отображаются только имена файлов без полного пути, что делает таблицы более компактными и читаемыми.

## Измененные файлы

### Представления
- `HomeCenter/Views/Admin/_AdminFolderContent.cshtml` - главная таблица админки
- `HomeCenter/Views/Admin/TestHistory.cshtml` - история изменений тестов

## Как это работает

### До изменений

**Колонка "Файл" показывала полный путь:**
```
География/Урок 5/Столицы мира.txt
Математика/Алгебра/Квадратные уравнения.txt
```

Это занимало много места и было трудно читать.

### После изменений

**Колонка "Файл" показывает только имя:**
```
Столицы мира.txt
Квадратные уравнения.txt
```

**При наведении курсора** появляется tooltip с полным путем к файлу.

## Преимущества

✅ **Компактность** - таблица занимает меньше места  
✅ **Читаемость** - легче найти нужный файл  
✅ **Доступность полного пути** - через tooltip при наведении  
✅ **Консистентность** - работает во всех таблицах админки  

## Где применяется

### 1. Главная таблица админки
**Путь:** `/Admin/Index`

Таблица со списком всех тестов теперь показывает только имена файлов.

### 2. История изменений тестов
**Путь:** `/Admin/TestHistory`

Колонка "Файл" показывает только имя файла. Полный путь доступен:
- В tooltip при наведении
- В колонке "Папка" (отдельно)

## Технические детали

### Вспомогательная функция

В обоих представлениях добавлена функция `GetFileName()`:

```csharp
@functions {
    string GetFileName(string filePath)
    {
        if (string.IsNullOrEmpty(filePath)) return "";
        return Path.GetFileName(filePath.Replace('/', Path.DirectorySeparatorChar)
                                        .Replace('\\', Path.DirectorySeparatorChar));
    }
}
```

### Использование

```html
<!-- Отображение с tooltip -->
<td><span title="@t.FileName">@GetFileName(t.FileName)</span></td>

<!-- Для code элементов -->
<td><code class="small" title="@h.FileName">@GetFileName(h.FileName)</code></td>
```

## Обратная совместимость

✅ Все существующие данные продолжают работать  
✅ Полный путь доступен через tooltip  
✅ Не требует изменений в базе данных  
✅ Не влияет на функциональность  

## Примеры

### Пример 1: Главная таблица админки

**Было:**
| Тема | Файл | Тип |
|------|------|-----|
| География → Урок 5 | География/Урок 5/test.txt | Тестирование |

**Стало:**
| Тема | Файл | Тип |
|------|------|-----|
| География → Урок 5 | test.txt ⓘ | Тестирование |

*(ⓘ - при наведении показывается полный путь)*

### Пример 2: История тестов

**Было:**
| Дата | Действие | Файл | Папка |
|------|----------|------|-------|
| 13.02.2026 10:30 | Изменён | `География/Урок 5/test.txt` | География/Урок 5 |

**Стало:**
| Дата | Действие | Файл | Папка |
|------|----------|------|-------|
| 13.02.2026 10:30 | Изменён | `test.txt` ⓘ | География/Урок 5 |

## Дополнительные улучшения (будущее)

Возможные улучшения в будущем:
- Сортировка по имени файла
- Фильтрация по расширению файла
- Группировка по папкам с сворачиванием
- Иконки для разных типов файлов
